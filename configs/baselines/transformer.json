{
  "model": "transformer",
  "architecture": { "n_leads": 12, "d_model": 128, "n_heads": 8, "num_layers": 4,
                     "dim_feedforward": 256, "dropout": 0.3, "attn_pool_size": 25 },
  "training": { "optimizer": "adam", "lr": 1e-3, "weight_decay": 1e-4, "grad_clip_norm": 1.0,
                "lr_schedule": "cosine_annealing_with_warmup", "warmup_epochs": 5,
                "max_epochs": 50, "early_stopping_patience": 10, "batch_size": 256 },
  "seed": 42,
  "reference_params_million": 14.1,
  "notes": "Table 8/9 baseline. Standard encoder-only Transformer over the lead dimension. attn_pool_size=25 downsamples T=5000->200 before self-attention -- required for tractability at batch_size=256, see models/baselines.py:TransformerBaseline."
}
